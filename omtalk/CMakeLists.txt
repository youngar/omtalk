if(OM_COMP_OMTALK_CORE)
  add_subdirectory(core)
endif()
if(OM_COMP_OMTALK_COMPILER)
  add_subdirectory(ir)
endif()
if(OM_COMP_OMTALK_PARSER)
  add_subdirectory(parser)
endif()
add_subdirectory(test)
add_subdirectory(tools)
add_subdirectory(vm)
