 #ifndef OMTALK_UTIL_HASH_H
 #define OMTALK_UTIL_HASH_H

#include <vector>

namespace omtalk {

/// Maps the hash value of a key to an entry.  Does not support lookup
/// of keys which are not entries.
template <typename V>
class StaticHashMap {
    

};


/// Maps a key to a value.
template<typename K, typename V>
class StaticMap {


};

}
 #endif